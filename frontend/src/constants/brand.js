// The product's display name, in one place.
//
// Deliberately generic: nothing on screen should identify whose business this
// is. Anyone who sees the login page, a browser tab, an emailed sheet or an
// exported file learns only that it's an expense tracker.
//
// To rename the whole app later, change these three lines — nothing else.
export const APP_NAME = "Expense Tracker";
export const APP_TAGLINE = "Where the money is going";
export const FILE_PREFIX = "expenses";
