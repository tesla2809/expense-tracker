// Server-side half of the product name — see frontend/src/constants/brand.js.
//
// This is what shows up OUTSIDE the app: the "from" name on emails, email
// subject lines, and the filename of any sheet that gets emailed or exported.
// Keep it generic; an email subject is the easiest way for the business name
// to end up somewhere it wasn't meant to go.
export const APP_NAME = "Expense Tracker";
export const FILE_PREFIX = "expenses";
